# TODO - Perbaikan Docker Compose Coolify Healthcheck

- [x] Perbaiki healthcheck MongoDB agar menggunakan autentikasi root.
- [x] Perkuat konfigurasi `MONGO_URL` backend agar konsisten ke service `mongodb`.
- [x] Tambahkan `extra_hosts` untuk kompatibilitas resolusi host internal.
- [x] Review akhir konfigurasi `docker-compose.yml`.
